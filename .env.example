# ============================================
# Pocket Assistant AI - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# Run the app with: npm run start:dev
# Run Langfuse (optional): docker-compose up -d → http://localhost:31111

# --- Required ---
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_from_botfather
OPENROUTER_API_KEY=your_openrouter_api_key

# --- Optional: Voice Transcription (Groq) ---
# Get your API key from https://console.groq.com
GROQ_API_KEY=your_groq_api_key

# --- Optional: MCP Integrations ---
ZAPIER_MCP_TOKEN=your_zapier_mcp_token

# --- Optional: Multi-Channel Messaging ---
# Enable API channel alongside Telegram (for REST API clients)
# ENABLE_API_CHANNEL=true

# --- Optional: ChromaDB Vector Store ---
# Required for semantic memory search (long-term memory)
# Run ChromaDB: docker compose up chromadb -d
CHROMA_HOST=http://localhost:8100

# --- Optional: Langfuse Observability ---
# Get keys from Langfuse dashboard (Cloud or self-hosted at http://localhost:31111)
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...

# Langfuse Cloud:
# LANGFUSE_HOST=https://cloud.langfuse.com

# Self-hosted Langfuse (docker-compose up -d, then use):
LANGFUSE_HOST=http://localhost:31111

# --- Docker: Langfuse secrets (only for docker-compose) ---
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://localhost:31111
